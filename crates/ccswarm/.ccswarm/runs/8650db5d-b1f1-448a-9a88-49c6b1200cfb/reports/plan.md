## Task Analysis Result

### Input Received
```
invalid-command-that-does-not-exist
```

### Assessment

The provided task description `invalid-command-that-does-not-exist` is **not a valid requirement**. It does not describe:

- A feature to implement
- A bug to fix
- A refactoring to perform
- A configuration change
- Any actionable work item

### What's Needed

To create an implementation plan, I need a task description that includes at minimum:

1. **What** — A clear description of the desired change (e.g., "Add a `status` subcommand to ccswarm CLI that shows running agents")
2. **Why** — The motivation or problem being solved (e.g., "Users need visibility into agent state without checking logs")

Optional but helpful:
- **Acceptance criteria** — How to verify the task is complete
- **Constraints** — Performance requirements, compatibility needs, etc.
- **Context** — Related issues, PRs, or prior discussions

### Recommendation

Please provide a valid task description and re-run the planning process.

---

[STEP:0] - success
