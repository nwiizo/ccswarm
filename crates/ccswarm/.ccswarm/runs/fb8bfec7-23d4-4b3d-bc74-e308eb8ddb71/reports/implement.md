## Assessment

The task provided is `invalid-command-that-does-not-exist`, which is not a valid or actionable implementation request. The planning step already confirmed this:

> **No implementation plan can be created** because there is no meaningful task to analyze. The input appears to be a placeholder, test string, or erroneous input rather than an actual task description.

There is nothing to implement. I cannot proceed without a concrete task description.

**Examples of actionable tasks:**
- "Add a `--verbose` flag to the CLI"
- "Fix session timeout handling in ai-session"
- "Implement DAG dependency resolution for workflows"
- "Add retry logic to the Claude Code bridge"

Please provide a real task description to proceed.

[STEP:1]
