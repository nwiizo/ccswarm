## Task Analysis Result

### Input Received
```
invalid-command-that-does-not-exist
```

### Assessment

The provided task description `invalid-command-that-does-not-exist` is **not a valid requirement**. It does not describe:

- A feature to implement
- A bug to fix
- A refactoring to perform
- A configuration change
- Any actionable work item

### What's Needed

To create an implementation plan, I need a task description that includes at minimum:

1. **What** — A clear description of the desired change (e.g., "Add a `health-check` CLI command that verifies all agent connections")
2. **Why** — The motivation or problem being solved (e.g., "Users need a way to diagnose connection issues")
3. **Scope** — Any constraints or boundaries (e.g., "Only for the ccswarm crate, no ai-session changes")

### Example Valid Tasks

```
"Add a --dry-run flag to the pipeline run command"
"Fix timeout handling when Claude CLI exceeds 600s in implement movement"
"Refactor AgentMailbox to use channel-based pattern instead of DashMap"
```

Please provide a valid task description for analysis.

[STEP:0]
