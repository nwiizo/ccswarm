## Status Report

The task cannot be implemented because **no valid task was provided**.

The input was:
```
invalid-command-that-does-not-exist
```

The planning step already confirmed this is not an actionable requirement:

> **No implementation plan can be created** because there is no actionable requirement to analyze.

### What I need to proceed

A valid task description with:
- **What** needs to be built or changed
- **Why** — the motivation or problem being solved
- **Acceptance criteria** — how to know it's done

**Example**: *"Add a `--dry-run` flag to the `pipeline run` command that validates the piece YAML without executing movements"*

There is nothing to implement, no code to write, and no tests to create for an invalid/empty task.

[STEP:1]
