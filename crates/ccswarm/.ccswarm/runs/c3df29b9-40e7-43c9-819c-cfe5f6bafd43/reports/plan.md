## Task Analysis Result

### Input Evaluation

The provided task is: `invalid-command-that-does-not-exist`

This is **not a valid task description**. It does not describe:
- A feature to implement
- A bug to fix
- A refactoring to perform
- A configuration change
- Any actionable requirement

### Conclusion

Unable to create an implementation plan because no meaningful requirement was provided. A valid task description should specify:

1. **What** needs to be done (e.g., "Add a `doctor` subcommand that checks system prerequisites")
2. **Why** it's needed (e.g., "Users need to verify their environment before running pipelines")
3. **Acceptance criteria** (e.g., "Command exits 0 when all checks pass, exits 1 with diagnostic output otherwise")

Please provide a concrete task description to proceed with analysis and planning.

[STEP:0]
