## Implementation Result

The plan from the previous step clearly identified that the task `invalid-command-that-does-not-exist` is **not a valid or actionable requirement**. There is no concrete feature, bug fix, refactoring, or change request described.

As the implementer, I cannot proceed with implementation because:

1. **No valid task exists** — the input is a literal invalid command string, not a specification
2. **The plan produced no actionable steps** — the planner correctly identified this as a non-task
3. **Implementing by guessing is prohibited** — per my constraints, I must report unclear points rather than guess

### What's needed

Please provide a concrete task description such as:
- "Add a `--timeout` flag to the pipeline runner"
- "Fix session restore failing when snapshot is corrupted"  
- "Implement retry logic for Claude Code CLI execution"

No files were read, modified, or created. No tests were written. No code changes were made.

[STEP:1]
