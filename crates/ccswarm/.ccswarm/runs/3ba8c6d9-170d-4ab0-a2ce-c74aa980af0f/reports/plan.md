## Analysis Result

The provided task description is `invalid-command-that-does-not-exist` — this is not a valid or actionable requirement. It appears to be either:

1. **A placeholder/test input** — no actual feature, bug fix, or change request is described
2. **A literal invalid command** — there is no context about what should be implemented, modified, or fixed

### What I Need to Create a Plan

To produce a meaningful implementation plan, I need a task description that includes:

- **What** needs to be built, fixed, or changed
- **Why** the change is needed (context/motivation)
- **Any constraints** or acceptance criteria

### No Action Taken

Since there is no valid task to analyze, no code investigation or planning was performed. No files were read or modified.

---

Please provide a concrete task description (e.g., "Add a `--timeout` flag to the pipeline runner" or "Fix session restore failing when snapshot is corrupted") and I will:

1. Investigate the relevant existing code
2. Identify impact scope and affected modules
3. Design the implementation approach
4. Create step-by-step implementation guidelines

[STEP:0]
