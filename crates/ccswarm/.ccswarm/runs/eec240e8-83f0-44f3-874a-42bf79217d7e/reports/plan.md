## Analysis Result

The provided task is not a valid requirement:

```
invalid-command-that-does-not-exist
```

This appears to be a non-existent command or placeholder text, not an actionable task description. There is no requirement to analyze, no unknowns to investigate, and no implementation to plan.

**To proceed, I need an actual task description** such as:
- A feature to implement (e.g., "Add DAG visualization to the CLI")
- A bug to fix (e.g., "Pipeline timeout not respected in movement execution")
- A refactoring goal (e.g., "Extract session retry logic into a shared utility")

Without a valid task, no implementation plan can be created.

[STEP:0]
