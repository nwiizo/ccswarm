## Analysis Result

The provided task is not a valid requirement or command:

```
invalid-command-that-does-not-exist
```

This input does not describe:
- A feature to implement
- A bug to fix
- A refactoring task
- A documentation update
- Any actionable development work

### What I Need

To create an implementation plan, please provide one of the following:

1. **A feature request** — e.g., "Add DAG visualization to the CLI"
2. **A bug report** — e.g., "Pipeline fails when task description exceeds 500 words"
3. **A refactoring task** — e.g., "Extract session retry logic into a shared utility"
4. **A command/subcommand to implement** — e.g., "Add `ccswarm metrics` command"

Without a valid task description, no investigation or planning can proceed.

[STEP:0]
