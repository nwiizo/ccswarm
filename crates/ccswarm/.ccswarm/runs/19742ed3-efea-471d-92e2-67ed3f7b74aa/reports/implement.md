## Report

The plan step correctly identified that the provided task is invalid:

```
invalid-command-that-does-not-exist
```

This is not a valid task description. It does not describe:
- A feature to implement
- A bug to fix
- A refactoring task
- A documentation update
- Any actionable development work

**There are no planned changes to implement.** The plan explicitly states: "Without a valid task description, no investigation or planning can proceed."

I cannot implement according to the plan because the plan contains no implementation steps — it is a request for clarification.

### What is needed

To proceed, please provide an actual task such as:
1. **A feature request** — e.g., "Add DAG visualization to the CLI"
2. **A bug report** — e.g., "Pipeline fails when task description exceeds 500 words"
3. **A refactoring task** — e.g., "Extract session retry logic into a shared utility"
4. **A command/subcommand to implement** — e.g., "Add `ccswarm metrics` command"

[STEP:1]
