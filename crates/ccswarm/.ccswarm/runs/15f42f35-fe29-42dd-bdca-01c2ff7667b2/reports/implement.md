## Assessment

The task `invalid-command-that-does-not-exist` is not a valid or actionable implementation request. The planning step already confirmed this:

> **No implementation plan can be produced from the current input.** Please provide a concrete task description.

There is nothing to implement. The task string does not describe:
- A feature to build
- A bug to fix
- A refactoring to perform
- Any concrete code change

I cannot implement by guessing — this would violate the constraint "Don't implement by guessing; report unclear points."

**Please provide a concrete task description** such as:
- "Add a `--dry-run` flag to the pipeline command"
- "Fix timeout errors when task descriptions exceed 500 words"
- "Implement health check endpoint to ai-session-server"

[STEP:1]
